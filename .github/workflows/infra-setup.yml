name: Infra Setup (One-Time or On Change)

on:
  workflow_dispatch:  # Manual trigger
  push:
    branches:
      - main
    paths:
      - 'cfn/**'
      - '.github/workflows/infra-setup.yml'

jobs:
  deploy-infra:
    runs-on: ubuntu-latest

    env:
      AWS_REGION: ap-south-1
      ENV: prod
      AWS_ACCOUNT_ID: ${{ secrets.AWS_ACCOUNT_ID }}
      MODEL_BUCKET: ${{ secrets.AWS_ACCOUNT_ID }}-prod-movenet-models
      KAGGLE_USERNAME: ${{ secrets.KAGGLE_USERNAME }}
      KAGGLE_KEY: ${{ secrets.KAGGLE_KEY }}

    permissions:
      contents: read
      id-token: write

    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4

      - name: 🧾 Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: 🚀 Deploy Model Bucket Stack
        run: |
          aws cloudformation deploy \
            --template-file cfn/model-bucket.yml \
            --stack-name movenet-model-bucket \
            --capabilities CAPABILITY_NAMED_IAM \
            --region $AWS_REGION \
            --parameter-overrides Environment=$ENV AWSAccountId=$AWS_ACCOUNT_ID

      - name: 📊 Deploy Logs Bucket Stack
        run: |
          aws cloudformation deploy \
            --template-file cfn/logs-bucket.yml \
            --stack-name movenet-logs-bucket \
            --capabilities CAPABILITY_NAMED_IAM \
            --region $AWS_REGION \
            --parameter-overrides Environment=$ENV AWSAccountId=$AWS_ACCOUNT_ID

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: 3.10

      - name: 📦 Install Python dependencies
        run: |
          pip install boto3 kaggle python-dotenv requests

      - name: 🔄 Upload latest MoveNet model to S3
        run: |
          python scripts/upload_versioned_model.py

      - name: 🌐 Deploy Frontend CloudFront Stack
        run: |
          aws cloudformation deploy \
            --template-file cfn/frontend-cloudfront.yml \
            --stack-name movenet-frontend-stack \
            --capabilities CAPABILITY_NAMED_IAM \
            --region $AWS_REGION \
            --parameter-overrides Environment=$ENV AWSAccountId=$AWS_ACCOUNT_ID

      - name: 🌐 Deploy Model CloudFront Stack
        run: |
          aws cloudformation deploy \
            --template-file cfn/model-cloudfront.yml \
            --stack-name movenet-model-cloudfront \
            --capabilities CAPABILITY_NAMED_IAM \
            --region $AWS_REGION \
            --parameter-overrides Environment=$ENV AWSAccountId=$AWS_ACCOUNT_ID
